Method 1: invalid parameter array placement-- should be main(String[] args)

public static void main(String args[]) {
    System.out.println("Hello World");
}

Method 2: invalid modifier-- should be "public static String"

public String myMethod() {
    return "Something";
}

Method 3: invalid-- should input "return x + y;", then back in the main method call this method with known variables

public static int addNumbers(int x, int y) {
   System.out.println(x + y);
}

Method 4: invalid: needs to be static

public double subtractNums(double x, double y, double z) {
    return "The answer is : " + (x + y + z);
}

Method 5: invalid parameter-- needs to be "printMessage(String[] args); also println should be println("message");

public static printMessage(String message) {
    System.out.println(message);
}

Method 6: valid

public static int() {
    return 5 + 5;
}

Method 7: invalid-- i think you need to widecast or "int num1, int num2", but variable int cannot be returned by short.

public static int value(short num1, short num2) {
    int answer = num1 * num2;
    return answer;
}